{
    "tasks": [
        {
            "label": "C++: Compilar y Ejecutar",
            "type": "shell",
            "command": "g++ -O2 ${file} -o ${fileDirname}/${fileBasenameNoExtension} && ${fileDirname}/${fileBasenameNoExtension}",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "reveal": "always",
                "panel": "new",
                "focus": true
            }
        }
    ],
    "version": "2.0.0"
}